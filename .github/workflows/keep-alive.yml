name: Supabase keep-alive

# Prevents the Supabase project from auto-pausing by making real EXTERNAL
# requests to the Supabase REST API on a schedule. Runs from GitHub's
# runners (outside the database), so it counts as external activity, unlike
# pg_cron, which runs inside Postgres and does not reset the inactivity timer.
# Frequency raised to every 6 hours on 13 Aug 2026 after Supabase flagged
# the project despite daily green pings, once per day was not enough.

on:
  schedule:
    # Every 6 hours: 00:00, 06:00, 12:00, 18:00 UTC
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API
        run: |
          echo "Pinging Supabase REST API…"
          status=$(curl --fail --silent --show-error \
            -o /dev/null -w "%{http_code}" \
            "https://pwqdzitsezblncmewxsf.supabase.co/rest/v1/enquiries?select=id&limit=1" \
            -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}")
          echo "HTTP status: $status"
